= form_for @dashboard do |f|
  - if @dashboard.errors.any?
    #error_explanation
      %h3
        = pluralize(@dashboard.errors.count, "error")
        prohibited this dashboard from being saved:
      %ul
        - @dashboard.errors.full_messages.each do |msg|
          %li= msg
  = f.label :title
  = f.text_field :title
  = f.label :desc
  = f.text_field :desc
  = f.label :stop_id_str
  = f.text_field :stop_id_str
  = f.submit
