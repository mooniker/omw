%h2 Location#index (listing of location requests)
%ul
  - @locations.each do |location|
    %li= link_to "#{location.lat}, #{location.lon}", location
%h2 Routes
%table
  %thead
    %tr
      - @route_attributes.each do |attribute|
        %th= attribute
  %tbody
  - @routes.each do |route|
    %tr
      - @route_attributes.each do |attribute|
        %td= route[attribute]
